const endpoint=(p)=>p; const val=(id)=>document.getElementById(id).value; const num=(id)=>Number(val(id));
document.querySelectorAll('.tab').forEach(b=>b.onclick=()=>{document.querySelectorAll('.tab,.panel').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.getElementById(b.dataset.tab).classList.add('active')});
const render=(id,data)=>document.getElementById(id).textContent=JSON.stringify(data,null,2);
async function api(path, body){const r=await fetch(endpoint(path),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});if(!r.ok)throw new Error(await r.text());return r.json()}
async function marketRegime(){try{render('marketResult',await api('/api/market-regime',{btc_change_24h:num('btcChange'),btc_volume_ratio:num('btcVolume'),sentiment:val('sentiment')}))}catch(e){render('marketResult',{error:e.message})}}
async function tokenomics(){try{render('tokenResult',await api('/api/tokenomics',{symbol:val('tokenAsset'),usage_score:num('usageScore'),liquidity_score:num('liqScore'),risk_score:num('riskScore')}))}catch(e){render('tokenResult',{error:e.message})}}
async function newsAnalyze(){try{render('newsResult',await api('/api/news-analyze',{headline:val('headline'),pre_move_pct_1h:num('preMove'),surprise_score:num('surprise'),source_reliability:num('source'),volume_multiple:num('volume'),account_equity:num('newsEquity'),normal_risk_pct:num('newsRisk'),entry:num('newsEntry'),stop:num('newsStop'),target:num('newsTarget')}))}catch(e){render('newsResult',{error:e.message})}}
async function positionSize(){try{render('positionResult',await api('/api/position-size',{account_equity:num('equity'),risk_per_trade_pct:num('risk'),entry_price:num('entry'),stop_price:num('stop'),target_price:num('target'),direction:val('direction')}))}catch(e){render('positionResult',{error:e.message})}}
async function dueDiligence(){try{render('ddResult',await api('/api/due-diligence',{market_cap:num('mc'),liquidity:num('liquidity'),top_holder_pct:num('holders'),team_transparency:num('team'),product_utility:num('utility'),audit_status:val('audit'),contract_verified:val('contract')==='true',unlock_risk:num('unlock'),community_quality:num('community')}))}catch(e){render('ddResult',{error:e.message})}}
const cards=[['BTC','Predictable scarcity','High liquidity'],['ETH','Issuance − burn','Settlement demand'],['SOL','Activity-sensitive','Higher beta / dilution risk']];document.getElementById('marketCards').innerHTML=cards.map(x=>`<article class="card"><h3>${x[0]}</h3><div class="metric">Research</div><p class="muted">${x[1]} · ${x[2]}</p></article>`).join('');
